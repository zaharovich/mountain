# Changelog - Изменения в проекте

## Дата: 16 ноября 2025

### ✅ Реализованные изменения

#### 1. **Изменен источник данных на удаленный JSON сервер**
- **Файл:** `js/dataManager.js`
- **Изменение:** URL для загрузки данных изменен с `http://localhost:3000/api/tt.json` на удаленный сервер
- **Варианты:**
  - Прямой доступ: `http://45.154.116.216:3240/tt.json` (требует CORS на сервере)
  - Через прокси: `http://localhost:3000/api/remote-tt.json` (обход CORS)
- **Текущая настройка:** Используется прокси для обхода CORS

#### 2. **Добавлена кнопка поиска в большем радиусе (20км)**
- **Файлы:** `index.html`, `js/app.js`, `css/styles.css`
- **Что добавлено:**
  - Кнопка появляется в правом верхнем углу карты
  - Показывается только когда текущий радиус поиска меньше 20км
  - При нажатии расширяет поиск до 20км
  - Автоматически скрывается после использования
  - Показывает toast уведомление с результатами

**Новый HTML элемент:**
```html
<div class="expand-search-btn" id="expandSearchBtnMain" style="display:none;">
    <button class="expand-search-btn__button">🔍 Шукати у радіусі 20 км</button>
</div>
```

**Новые функции в app.js:**
- `expandSearchFromMain()` - расширяет поиск с главного интерфейса
- `showToast()` - показывает уведомления пользователю

**Новые CSS стили:**
```css
.expand-search-btn - позиционирование кнопки
.expand-search-btn__button - стилизация кнопки
.toast-container - контейнер для уведомлений
.toast - стили toast уведомлений
```

#### 3. **Автоматическое использование ВСЕХ категорий продукции из JSON**
- **Статус:** Уже реализовано в исходном коде
- **Как работает:**
  - `dataManager.getProducts()` возвращает ВСЕ продукты из JSON файла
  - Фильтр категорий автоматически генерирует опции для всех продуктов
  - При загрузке удаленного JSON все новые категории автоматически появятся в фильтре

```javascript
// В app.js - инициализация фильтров
products.forEach(product => {
    const option = document.createElement('option');
    option.value = product.id;
    option.textContent = product.title;
    filterSelect.appendChild(option);
});
```

#### 4. **Добавлен прокси-сервер для обхода CORS**
- **Файл:** `server.js`
- **Новый роут:** `/api/remote-tt.json`
- **Функция:** Проксирует запросы к удаленному серверу, решая проблемы с CORS

```javascript
app.get('/api/remote-tt.json', async (req, res) => {
    // Загружает данные с http://45.154.116.216:3240/tt.json
    // И возвращает их с правильными CORS заголовками
});
```

#### 5. **Добавлена система toast уведомлений**
- **Файлы:** `js/app.js`, `css/styles.css`
- **Функциональность:**
  - Показывает уведомления о результатах расширенного поиска
  - Поддержка разных типов: success, error, info, warning
  - Автоматически исчезают через 3 секунды
  - Адаптивный дизайн для мобильных устройств

---

## 📋 Как использовать

### Запуск приложения:

1. Установите зависимости:
```bash
npm install
```

2. Запустите локальный сервер:
```bash
node server.js
```

3. Откройте в браузере:
```
http://localhost:3000
```

### Переключение между прямым доступом и прокси:

В файле `js/dataManager.js` выберите один из вариантов:

```javascript
// Вариант 1: Прямой доступ (если CORS настроен)
this.API_URL = 'http://45.154.116.216:3240/tt.json';

// Вариант 2: Через прокси (рекомендуется)
this.API_URL = 'http://localhost:3000/api/remote-tt.json';
```

---

## 🎯 Основные возможности

1. ✅ **Автоматическая загрузка данных** с удаленного сервера
2. ✅ **Все категории продукции** автоматически отображаются в фильтре
3. ✅ **Кнопка расширенного поиска** (до 20км)
4. ✅ **Адаптивный радиус поиска** (3/6/10/20км)
5. ✅ **Toast уведомления** о результатах
6. ✅ **Прокси-сервер** для обхода CORS
7. ✅ **Фильтрация по категориям** с Select2
8. ✅ **Интерактивная карта** Leaflet

---

## 🔧 Технические детали

### Структура JSON данных:
```json
{
  "cities": [
    { "name": "Харків", "id": "1" }
  ],
  "shops": [
    {
      "id": "1380",
      "name": "Магазин",
      "map": "https://www.google.com/maps/place/50.4265,30.5383",
      "products": [
        { "id": "130", "timestamp": "2025-11-10" }
      ]
    }
  ],
  "products": [
    { "id": "130", "title": "Горішки" }
  ]
}
```

### Адаптивный поиск:
- Начинает с радиуса 3км
- Автоматически расширяет до 6/10/20км если найдено мало магазинов
- Кнопка расширенного поиска доступна если текущий радиус < 20км

### CORS решение:
- Локальный прокси на Node.js/Express
- Автоматическая установка правильных заголовков
- Логирование запросов для отладки

---

## 📝 Тестирование

### Тест удаленного JSON:
Откройте в браузере: `http://localhost:3000/test-remote-json.html`

Это покажет:
- Статус загрузки данных
- Количество магазинов и продуктов
- Полную структуру JSON

---

## 🐛 Возможные проблемы и решения

### 1. CORS ошибка
**Проблема:** Браузер блокирует запросы к удаленному серверу
**Решение:** Используйте прокси (уже настроен)

### 2. Сервер недоступен
**Проблема:** Удаленный сервер не отвечает
**Решение:** Приложение автоматически использует кэш из localStorage

### 3. Категории не отображаются
**Проблема:** JSON имеет неправильную структуру
**Решение:** Проверьте структуру в test-remote-json.html

---

## 👨‍💻 Авторы изменений

- Анализ проекта и реализация: Claude Sonnet 4.5
- Дата: 16 ноября 2025
